[graph]
all-features = true

[advisories]
unmaintained = "workspace"
unused-ignored-advisory = "deny"
ignore = [
    { id = "RUSTSEC-2026-0085", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0086", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0087", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0088", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0089", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0091", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0092", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0093", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0094", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0095", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0096", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
    { id = "RUSTSEC-2026-0114", reason = "Upstream-blocked by extism 1.21.0's wasmtime ^41 dependency. The SDK runs vendor-provided 1Password core.wasm only; remove when extism/extism#901 is released." },
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "CDLA-Permissive-2.0",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.8

[sources]
unknown-registry = "deny"
unknown-git = "deny"
